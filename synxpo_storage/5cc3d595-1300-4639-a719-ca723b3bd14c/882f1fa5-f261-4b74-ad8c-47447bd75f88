version: "3.9"

services:
  secret-santa-bot:
    build: .
    container_name: secret-santa-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      SECRET_SANTA_DB_PATH: /app/data/secret_santa.db
    volumes:
      - secret-santa-data:/app/data
    # Uncomment the next line if you want to see logs in real time via `docker compose logs -f`
    # logging:
    #   driver: json-file
    #   options:
    #     max-size: "10m"
    #     max-file: "3"

volumes:
  secret-santa-data:

